#database 

## What is function
- Function takes input and generates an output.
- It's a set of steps that creates a single value.

## Type of function
- Aggregate
	- Operate on many records to produce 1 value.
	- `Avg()`
	- `Count()`
	- `Min()`
	- `Max()`
	- `Sum()`
- scalar